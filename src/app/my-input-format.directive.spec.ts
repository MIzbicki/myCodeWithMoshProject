import { MyInputFormatDirective } from './my-input-format.directive';

describe('MyInputFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new MyInputFormatDirective();
    expect(directive).toBeTruthy();
  });
});
